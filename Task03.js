'use strict';

// {

// 	const generateArray = (length, m, n, str) => {
// 		if (length < 0) return;
	
// 		if (n < m) return generateArray(length, n, m, str);
	
// 		const newArr = [...new Array(length)].map(
// 			() => Math.floor(Math.random() * (n + 1 - m)) + m
// 		);
	
// 		if (str === "even") {
// 			return newArr.filter((i) => !(i % 2));
// 		}
// 		if (str === "odd") {
// 			return newArr.filter((i) => i % 2);
// 		}
	
// 		return newArr;
// 	};
	
// 	console.log(generateArray(100,20,10,'even'));


// }
