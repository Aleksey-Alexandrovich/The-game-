'use strict';


// {

// 	const generateArray = (length, m, n) => {
// 		if (length < 0) return;
	
// 		if (n < m) return generateArray(length, n, m);
	
// 		const newArr = [...new Array(length)].map(
// 			() => Math.floor(Math.random() * (n + 1 - m)) + m
// 		);
	
	
// 		return newArr;
// 	};
	
// 	console.log(generateArray(100,80,-20));


// }


