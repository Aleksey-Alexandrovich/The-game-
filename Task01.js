'use strict';


// {
// 	const generateArray = (length) => {
// 		return [...new Array(length)].map(() => Math.round(Math.random() * 100))
// 	}

// 	const newRandom = generateArray(100);

// 	console.log(newRandom);

// }